Feature: Behave demo
  This is a basic test of Behave.
  First we write a scenario.
  Then whe hook the text of the scenarios with Python code
  Finally we execute the test case using Behave.
  
  Scenario: sum two numbers and check result
     Given 3 and 4
      when we add both numbers
      then result is 7
	  
